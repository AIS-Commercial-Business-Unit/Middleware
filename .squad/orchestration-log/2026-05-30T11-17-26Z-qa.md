# Orchestration Log — QA (2026-05-30T11:17:26Z)

**Agent:** QA  
**Spawn Manifest:** UC4 build  
**Assigned Task:** Create UC4 integration test suites for prs-appraisal-service

## Work Completed

### 1. Integration Test Suites

**GetAppraisalListIntegrationTest.java** (6 tests)
- SC-001: successful list retrieval with valid policy number
- SC-002: empty list for unknown policy number
- SC-003: @Work SQL path returns records correctly
- SC-004: DEIPDE07 timeout returns partialResult=true (30s timeout, @Timeout(40s) guard)
- SC-005: deduplication merges @Work + DEIPDE07 results by (streetAdr + policyNumber)
- SC-006: malformed request (empty policyNumber) returns HTTP 400

**GetAppraisalDocumentIntegrationTest.java** (6 tests)
- SC-001: successful document retrieval via RiskID WCF insured path (_RiskID_I)
- SC-002: successful document retrieval via RiskID WCF agent path (_RiskID_A)
- SC-003: successful document retrieval via DEIPDE07 MQ path (numeric key)
- SC-004: multi-message chunk aggregation with CRLF stripping
- SC-005: invalid key format returns HTTP 400
- SC-006: document not found returns HTTP 404

### 2. Test Infrastructure

- `@Tag("integration")` on all tests — excluded from default Maven test phase
- Uses `RestTemplate` to call `http://localhost:8090` directly (full stack verification)
- Tests exercise: Camel routes, JMS connections to Artemis, @Work/RiskID/DEIPDE07 stubs
- `@Timeout(40)` on SC-004 (DEIPDE07 timeout) prevents indefinite CI hang

### 3. Maven Configuration

**prs-appraisal-service pom.xml:**
- Added `spring-boot-starter-test` in test scope
- Added `integration-tests` Maven profile for running tests on demand

**Profile usage:**
- Default: `mvn test` excludes integration tests (fast unit tests only)
- On-demand: `mvn test -Dgroups=integration` runs full integration suite

### 4. Test Evidence Documentation

**UC4TestEvidence.md** created with:
- Fill-in table format for test run results
- Columns: test_case, scenario, input, expected_output, actual_output, status, log_excerpt
- Ready for QA to populate after first docker stack run

## API Contract Established

**GetAppraisalList:**
```json
POST /api/appraisals/list
Request: { "policyNumber": "..." }
Response: {
  "items": [ { "appraisalUid", "policyQuoteNbr", "streetAdr", "cityAdr", "stateCde", "zipAdr", "appraisalDte", "documenttype", "documentname", "documentkey" } ],
  "partialResult": boolean
}
```

**GetAppraisalDocument:**
```json
POST /api/appraisals/document
Request: { "documentKey": "..." }
Response: { "base64Pdf": "<no \\r\\n>", "contentType": "application/pdf" }
```

## Dependencies Satisfied

- ✅ Integration agent completed prs-appraisal-service routes
- ✅ DevOps wired docker-compose stack
- ✅ Backend built deipde07-mq-simulator

## Test Status

- ✅ 12 test cases written with API contracts
- ⏳ Verification pending: docker compose up → all services healthy
- ⏳ Test execution: `mvn test -Dgroups=integration` after stack confirms ready

## Next Steps

1. Stack verification: docker compose up
2. Integration test execution: verify all 12 tests pass
3. Evidence collection: populate UC4TestEvidence.md with results
4. Log verification: confirm structured logging emitted for each test path
