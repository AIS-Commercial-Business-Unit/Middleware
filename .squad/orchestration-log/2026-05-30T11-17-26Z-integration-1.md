# Orchestration Log — Integration (2026-05-30T11:17:26Z)

**Agent:** Integration-1  
**Spawn Manifest:** UC4 build  
**Assigned Task:** Rewrite prs-appraisal-service to correct UC4 scope (GetAppraisalList + GetAppraisalDocument)

## Work Completed

### 1. Scope Correction Implementation

**Removed** (incorrect scope):
- 5 saga route files (AppraisalReceivedSagaRoute, GenericStatusUpdateSagaRoute, SagaTimeoutRoute, StatusCode15CompletedSagaRoute, StatusCode6UWSagaRoute)
- Saga persistence layer (AppraisalSagaDocument, AppraisalSagaMongoRepository, AppraisalSagaRepositoryAdapter, AppraisalSagaRecord, AppraisalSagaRepository)
- Old gateway interfaces and stubs (PLUWGateway, PLAPRGateway, MasterpieceGateway, CustomerDBGateway)
- MongoConfig.java (OffsetDateTime converters)
- Old AppraisalController.java (status-update endpoints, saga list/detail)

**Built** (correct scope):

### 2. Route Implementation

1. **GetAppraisalListRoute** — scatter-gather pattern
   - `POST /api/appraisals/list` with `{"policyNumber": "..."}`
   - Parallel fan-out: direct:callAtWorkSQL + direct:callDEIPDE07MQList
   - Aggregates + deduplicates via AppraisalListAggregationStrategy
   - Returns appraisal list with partialResult flag

2. **GetAppraisalDocumentRoute** — content-based router
   - `POST /api/appraisals/document` with `{"documentKey": "..."}`
   - Routes by key format: _RiskID_I/A → RiskID WCF, numeric → DEIPDE07 MQ
   - Multi-message chunk aggregation with EBCDIC artifact stripping
   - Returns base64-encoded PDF

### 3. Supporting Infrastructure

- `AppraisalListMqPoller` — ConsumerTemplate poll loop with timeout detection
- `PdfChunkMqPoller` — ConsumerTemplate chunk aggregation with sentinel detection
- `AppraisalListAggregationStrategy` — merge and deduplicate @Work + DEIPDE07 results
- `AtWorkGateway` — fixture data for demo (POL-001-TEST, POL-002-TEST, POL-003-TEST)
- `RiskIdWcfGateway` — fixture PDF responses
- `JmsConfig` — Camel JMS component wiring to Spring Artemis
- `AppraisalAuditEventRoute` — publishes appraisal events to Kafka

### 4. Configuration Updates

**pom.xml additions:**
- spring-boot-starter-artemis (dev broker)
- camel-jms-starter
- camel-http-starter (future RiskID WCF)

**application.yml changes:**
- Removed saga timeout config, Kafka consumer config
- Added spring.artemis.* connection properties

## Dependencies Satisfied

- ✅ deipde07-mq-simulator backend available
- ✅ ActiveMQ Artemis configured via spring-boot-starter-artemis
- ✅ Routes ready for docker-compose wiring

## Test Status

- ⏳ Integration tests to be verified by QA agent
- Routes DSL correct for eventual production IBM MQ swap (config-only change)

## Next Steps

- QA: verify via GetAppraisalListIntegrationTest and GetAppraisalDocumentIntegrationTest
- DevOps: wire into docker-compose with Artemis broker
