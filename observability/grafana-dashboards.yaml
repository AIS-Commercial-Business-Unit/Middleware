apiVersion: 1

providers:
  - name: AIS Middleware Dashboards
    folder: Middleware
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
